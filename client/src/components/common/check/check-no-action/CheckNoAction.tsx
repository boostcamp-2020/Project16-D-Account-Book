import React from 'react';
import styled from 'styled-components';

const CheckNoActionWrapper = styled.div`
  box-sizing: border-box;
  padding-left: 35px;
  height: 25px;
`;

const CheckNoAction: React.FC = () => {
  return <CheckNoActionWrapper></CheckNoActionWrapper>;
};

export default CheckNoAction;
