import React from 'react';
import styled from 'styled-components';

const CheckNoActionTextWrapper = styled.p`
  text-align: center;
  height: 1rem;
`;

const CheckNoActionText: React.FC = () => {
  return <CheckNoActionTextWrapper></CheckNoActionTextWrapper>;
};

export default CheckNoActionText;
