import React from 'react';
import ExportButton from './ExportButton';

export default {
  title: 'export-button/ExportButton',
  component: ExportButton,
};

export const Default: React.FC = () => {
  return <ExportButton />;
};
